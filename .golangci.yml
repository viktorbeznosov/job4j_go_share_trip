version: "2"

linters:
  default: standard
  enable:
    - paralleltest
    - errcheck
    - staticcheck
  exclusions:
    rules:
      - path: internal/api/api_test/
        linters:
          - paralleltest